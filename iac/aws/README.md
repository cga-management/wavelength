# AWS landing zone (OpenTofu)

Later. Built after Azure is working, to the same structure: `providers.tf`, `backend.tf` (S3 + lock), `variables.tf` (account, region, naming), `main.tf`. Maps the same intents to AWS - App Runner or ECS-Fargate for compute, Secrets Manager, ECR, CloudWatch, and SCP/guardrails.

**Status:** not started. Azure first.
